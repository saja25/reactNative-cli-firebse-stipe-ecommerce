import { StyleSheet, Dimensions } from "react-native";

const Styles = StyleSheet.create({
  container: {
    // backgroundColor: "red",
  },
  image: {
    // width: "100%",
    height: 460,
    resizeMode: "cover",
  },
});
export default Styles;
